merge into STATUSES (ID, NAME)
    values (1, 'WAITING');
merge into STATUSES (ID, NAME)
    values (2, 'APPROVED');
merge into STATUSES (ID, NAME)
    values (3, 'REJECTED');
merge into STATUSES (ID, NAME)
    values (4, 'CANCELETED');
